Index: mullvad-daemon/build.rs
--- mullvad-daemon/build.rs.orig
+++ mullvad-daemon/build.rs
@@ -51,6 +51,10 @@
 }
 
 fn commit_date() -> String {
+    println!("cargo::rerun-if-env-changed=MULLVAD_BUILD_FROM_ARCHIVE");
+    if env::var("MULLVAD_BUILD_FROM_ARCHIVE").as_deref() == Ok("1") {
+        return "release archive".to_owned();
+    }
     let output = Command::new("git")
         .args(["log", "-1", "--date=short", "--pretty=format:%cd"])
         .output()
@@ -67,6 +71,7 @@
     Macos,
     Linux,
     Android,
+    Openbsd,
 }
 
 fn target_os() -> Os {
@@ -76,6 +81,7 @@
         "macos" => Os::Macos,
         "linux" => Os::Linux,
         "android" => Os::Android,
+        "openbsd" => Os::Openbsd,
         _ => panic!("Unsupported target os: {target_os}"),
     }
 }
