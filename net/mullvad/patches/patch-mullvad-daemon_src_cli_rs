Index: mullvad-daemon/src/cli.rs
--- mullvad-daemon/src/cli.rs.orig
+++ mullvad-daemon/src/cli.rs
@@ -113,7 +113,7 @@
 
         command_flags
             .into_iter()
-            .find_map(|(flag, command)| flag.then_some(command))
+            .find_map(|(flag, command): (bool, Command)| flag.then_some(command))
             .unwrap_or(Command::Daemon)
     }
 }
