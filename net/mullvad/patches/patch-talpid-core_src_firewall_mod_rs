Index: talpid-core/src/firewall/mod.rs
--- talpid-core/src/firewall/mod.rs.orig
+++ talpid-core/src/firewall/mod.rs
@@ -21,6 +21,9 @@
         /// Firewall implementation for desktop Linux
         pub mod linux;
         use linux as imp;
+    } else if #[cfg(target_os = "openbsd")] {
+        mod openbsd;
+        use openbsd as imp;
     } else if #[cfg(target_os = "android")] {
         /// Firewall implementation for Android
         mod android;
