SPDX-License-Identifier: GPL-3.0-or-later

Index: talpid-core/src/firewall/openbsd.rs
--- /dev/null
+++ talpid-core/src/firewall/openbsd.rs
@@ -0,0 +1,17 @@
+//! Fail-closed integration boundary, pending native PF state management.
+//! Candidate policy generation lives in talpid-openbsd. Never install it
+//! without verifying anchor reachability, skip rules and pre-existing states.
+use super::{FirewallArguments, FirewallPolicy};
+
+#[derive(Debug, thiserror::Error)]
+#[error("OpenBSD PF integration is incomplete; VPN startup is prohibited")]
+pub struct Error;
+
+pub struct Firewall;
+
+impl Firewall {
+    pub fn from_args(_args: FirewallArguments) -> Result<Self, Error> { Err(Error) }
+    pub fn new() -> Result<Self, Error> { Err(Error) }
+    pub fn apply_policy(&mut self, _policy: FirewallPolicy) -> Result<(), Error> { Err(Error) }
+    pub fn reset_policy(&mut self) -> Result<(), Error> { Err(Error) }
+}
