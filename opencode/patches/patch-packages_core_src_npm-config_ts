Fix CJS/ESM interop for @npmcli/config. Under Node the named
imports from this CommonJS module resolve keys on the default
export, not named exports. Use a default import and destructure.

SPDX-License-Identifier: MIT
Index: packages/core/src/npm-config.ts
--- packages/core/src/npm-config.ts.orig
+++ packages/core/src/npm-config.ts
@@ -4,7 +4,8 @@
 // @ts-expect-error npm does not publish types for this internal config API.
 import Config from "@npmcli/config"
 // @ts-expect-error npm does not publish types for this internal config API.
-import { definitions, flatten, nerfDarts, shorthands } from "@npmcli/config/lib/definitions/index.js"
+import defsModule from "@npmcli/config/lib/definitions/index.js"
+const { definitions, flatten, nerfDarts, shorthands } = defsModule as any
 import { Effect } from "effect"
 
 const npmPath = fileURLToPath(new URL("..", import.meta.url))
