$OpenBSD$

SPDX-License-Identifier: MIT

Disable Bun-backed plugin support because Bun is not available on
OpenBSD. The CLI plugin install command must fail closed before package
resolution or config patching can run.

Index: packages/opencode/src/cli/cmd/plug.ts
--- packages/opencode/src/cli/cmd/plug.ts.orig
+++ packages/opencode/src/cli/cmd/plug.ts
@@ -4,7 +4,7 @@ import { Effect } from "effect"
 import { ConfigPaths } from "@/config/paths"
 import { Global } from "@opencode-ai/core/global"
 import { installPlugin, patchPluginConfig, readPluginManifest } from "../../plugin/install"
-import { resolvePluginTarget } from "../../plugin/shared"
+import { PLUGIN_DISABLED_MESSAGE } from "../../plugin/shared"
 import { errorMessage } from "../../util/error"
 import { Filesystem } from "@/util/filesystem"
 import { Process } from "@/util/process"
@@ -205,26 +205,7 @@ export const PluginCommand = effectCmd({
       return
     }
 
-    UI.empty()
-    intro(`Install plugin ${mod}`)
-
-    const run = createPlugTask({
-      mod,
-      global: Boolean(args.global),
-      force: Boolean(args.force),
-    })
-
-    const ctx = yield* InstanceRef
-    if (!ctx) return
-    const ok = yield* Effect.promise(() =>
-      run({
-        vcs: ctx.project.vcs,
-        worktree: ctx.worktree,
-        directory: ctx.directory,
-      }),
-    )
-
-    outro("Done")
-    if (!ok) process.exitCode = 1
+    UI.error(PLUGIN_DISABLED_MESSAGE)
+    process.exitCode = 1
   }),
 })
