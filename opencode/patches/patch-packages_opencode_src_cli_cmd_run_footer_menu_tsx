Replace Bun.stringWidth with the string-width package. The OpenBSD port
runs under Node and must not require the Bun global at runtime.

Index: packages/opencode/src/cli/cmd/run/footer.menu.tsx
--- packages/opencode/src/cli/cmd/run/footer.menu.tsx.orig
+++ packages/opencode/src/cli/cmd/run/footer.menu.tsx
@@ -1,5 +1,6 @@
 /** @jsxImportSource @opentui/solid */
 import { TextAttributes, type ColorInput } from "@opentui/core"
+import stringWidth from "string-width"
 import { useTerminalDimensions } from "@opentui/solid"
 import { createEffect, createMemo, createSignal, type Accessor } from "solid-js"
 import { transparent, type RunFooterTheme } from "./theme"
@@ -196,7 +197,7 @@ export function createFooterMenuState(props: {
       ...props
         .items()
         .filter((item) => item.description)
-        .map((item) => Bun.stringWidth(item.display)),
+        .map((item) => stringWidth(item.display)),
     )
     return width === 0 ? 0 : width + 2
   })
@@ -205,14 +206,14 @@ export function createFooterMenuState(props: {
       return ""
     }
 
-    return " ".repeat(Math.max(1, descriptionColumn() - Bun.stringWidth(item.display)))
+    return " ".repeat(Math.max(1, descriptionColumn() - stringWidth(item.display)))
   }
   const descriptionText = (item: RunFooterMenuItem) => {
     if (!item.description) {
       return
     }
 
-    const footerWidth = item.footer ? Bun.stringWidth(item.footer) + 1 : 0
+    const footerWidth = item.footer ? stringWidth(item.footer) + 1 : 0
     const available =
       term().width -
       (border() ? 1 : 0) -
