Remove the suggestion to use the rg (ripgrep) command from the
grep tool description. ripgrep is not a dependency of the
OpenBSD port and may not be available on the user's system.

SPDX-License-Identifier: MIT
Index: packages/opencode/src/tool/grep.txt
--- packages/opencode/src/tool/grep.txt.orig
+++ packages/opencode/src/tool/grep.txt
@@ -4,5 +4,5 @@
 - Filter files by pattern with the include parameter (eg. "*.js", "*.{ts,tsx}")
 - Returns file paths and line numbers with matching lines
 - Use this tool when you need to find files containing specific patterns
-- If you need to identify/count the number of matches within files, use the Bash tool with `rg` (ripgrep) directly. Do NOT use `grep`.
+- If you need to identify/count the number of matches within files, use the Bash tool. Do NOT use `grep`.
 - When you are doing an open-ended search that may require multiple rounds of globbing and grepping, use the Task tool instead
