Replace Bun.stringWidth with the string-width package. The OpenBSD port
runs under Node and must not require the Bun global at runtime.

Index: packages/tui/src/component/prompt/index.tsx
--- packages/tui/src/component/prompt/index.tsx.orig
+++ packages/tui/src/component/prompt/index.tsx
@@ -9,6 +9,7 @@ import {
   type Renderable,
 } from "@opentui/core"
 import type { CommandContext } from "@opentui/keymap"
+import stringWidth from "string-width"
 import { createEffect, createMemo, onMount, createSignal, onCleanup, on, Show, Switch, Match } from "solid-js"
 import "opentui-spinner/solid"
 import path from "path"
@@ -504,7 +505,7 @@ export function Prompt(props: {
             parts: updatedNonTextParts,
           })
           restoreExtmarksFromParts(updatedNonTextParts)
-          input.cursorOffset = Bun.stringWidth(normalized)
+          input.cursorOffset = stringWidth(normalized)
         },
       },
       {
