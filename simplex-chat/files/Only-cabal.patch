$OpenBSD$

Relax the deepseq bound to match the GHC 9.10 toolchain used by the port.

--- ../Only-0.1/Only.cabal
+++ ../Only-0.1/Only.cabal
@@ -26,7 +26,7 @@
                    , Safe
 
   build-depends:     base    >= 4.5 && <5
-                   , deepseq >= 1.1 && <1.5
+                   , deepseq >= 1.1 && <1.6
 
   if impl(ghc == 7.4.*)
      build-depends: ghc-prim
