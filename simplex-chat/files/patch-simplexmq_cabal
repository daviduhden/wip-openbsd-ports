SPDX-License-Identifier: AGPL-3.0-only

Use the crypton-era ASN.1 packages, time-hourglass, ram byte arrays,
and the tls 2.4 API targeted by the OpenBSD compatibility patches.

Index: simplexmq.cabal
--- simplexmq.cabal.orig
+++ simplexmq.cabal
@@ -346,8 +346,8 @@
         bcrypt
   build-depends:
       aeson ==2.2.*
-    , asn1-encoding ==0.9.*
-    , asn1-types ==0.3.*
+    , crypton-asn1-encoding ==0.10.*
+    , crypton-asn1-types ==0.4.*
     , async ==2.2.*
     , attoparsec ==0.14.*
     , base >=4.14 && <5
@@ -363,12 +363,12 @@
     , data-default ==0.7.*
     , directory ==1.3.*
     , filepath ==1.4.*
-    , hourglass ==0.2.*
+    , time-hourglass ==0.3.*
     , http-types ==0.12.*
     , http2 >=4.2.2 && <4.3
     , iproute ==1.7.*
     , iso8601-time ==0.1.*
-    , memory ==0.18.*
+    , ram ==0.22.*
     , mtl >=2.3.1 && <3.0
     , network >=3.1.2.7 && <3.2
     , network-info ==0.2.*
@@ -381,7 +381,7 @@
     , stm ==2.5.*
     , time ==1.12.*
     , time-manager ==0.0.*
-    , tls >=1.9.0 && <1.10
+    , tls >=2.4.3 && <2.5
     , transformers ==0.6.*
     , unliftio ==0.2.*
     , unliftio-core ==0.2.*
@@ -617,7 +617,7 @@
     , ini
     , iso8601-time
     , main-tester ==0.2.*
-    , memory
+    , ram
     , mtl
     , network
     , QuickCheck ==2.14.*
@@ -629,7 +629,7 @@
     , text
     , time
     , timeit ==2.0.*
-    , tls >=1.9.0 && <1.10
+    , tls >=2.4.3 && <2.5
     , transformers
     , unliftio
     , unliftio-core
