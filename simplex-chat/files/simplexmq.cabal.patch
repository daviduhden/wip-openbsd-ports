$OpenBSD$

Relax SimpleXMQ bounds for GHC 9.10 and current upstream library versions.
Copy hidden web assets and the xftp static page into Cabal's temp source tree so file-embed can see them.

--- simplexmq.cabal.orig
+++ simplexmq.cabal
@@ -24,6 +24,8 @@
     CHANGELOG.md
     cbits/sha512.h
     cbits/sntrup761.h
+    apps/common/Web/static/.well-known/apple-app-site-association
+    apps/common/Web/static/.well-known/assetlinks.json
     apps/common/Web/static/index.html
     apps/common/Web/static/link.html
     apps/common/Web/static/media/apk_icon.png
@@ -53,6 +55,7 @@
     apps/common/Web/static/media/testflight.png
     apps/xftp-server/static/media/xftp-protocol.svg
     apps/xftp-server/static/media/xftp-protocol-dark.svg
+    apps/xftp-server/static/file.html
     apps/xftp-server/static/xftp-web-bundle/crypto.worker.js
     apps/xftp-server/static/xftp-web-bundle/index.css
     apps/xftp-server/static/xftp-web-bundle/index.js
@@ -312,8 +315,8 @@
     , base >=4.14 && <5
     , base64-bytestring >=1.0 && <1.3
     , composition ==1.0.*
-    , constraints >=0.12 && <0.14
-    , containers ==0.6.*
+    , constraints >=0.14 && <0.15
+    , containers >=0.6 && <0.8
     , crypton ==0.34.*
     , crypton-x509 ==1.7.*
     , crypton-x509-store ==1.6.*
@@ -324,7 +327,7 @@
     , filepath ==1.4.*
     , hourglass ==0.2.*
     , http-types ==0.12.*
-    , http2 >=4.2.2 && <4.3
+    , http2 >=5.1 && <5.4
     , iproute ==1.7.*
     , iso8601-time ==0.1.*
     , memory ==0.18.*
@@ -334,13 +337,13 @@
     , network-transport ==0.5.6
     , network-udp ==0.0.*
     , random >=1.1 && <1.3
-    , scientific ==0.3.7.*
+    , scientific >=0.3.8 && <0.4
     , simple-logger ==0.1.*
     , socks ==0.6.*
     , stm ==2.5.*
     , time ==1.12.*
-    , time-manager ==0.0.*
-    , tls >=1.9.0 && <1.10
+    , time-manager >=0.2 && <0.3
+    , tls >=2.0 && <2.3
     , transformers ==0.6.*
     , unliftio ==0.2.*
     , unliftio-core ==0.2.*
@@ -359,9 +362,9 @@
       , temporary ==1.3.*
       , wai >=3.2 && <3.3
       , wai-app-static >=3.1 && <3.2
-      , warp ==3.3.30
+      , warp >=3.3.31 && <3.5
       , warp-tls ==3.4.7
-      , websockets ==0.12.*
+      , websockets >=0.12 && <0.14
       , zlib >=0.6 && <0.8
   if flag(client_postgres) || flag(server_postgres)
     build-depends:
@@ -380,8 +383,8 @@
         hex-text ==0.1.*
   if impl(ghc >= 9.6.2)
     build-depends:
-        bytestring ==0.11.*
-      , template-haskell ==2.20.*
+        bytestring >=0.11 && <0.13
+      , template-haskell >=2.20 && <2.23
       , text >=2.0.1 && <2.2
   if impl(ghc < 9.6.2)
     build-depends:
