$OpenBSD$

OpenBSD patch for patch-terminal-underlining.

$OpenBSD$

Handle new Underlining constructor in ConsoleLayer. Underlining is a text
decoration, not a color layer — use foreground as a sensible default.

--- src/Simplex/Chat/Terminal/Output.hs.orig
+++ src/Simplex/Chat/Terminal/Output.hs
@@ -409,4 +409,5 @@
     layer = \case
       Foreground -> foreground
       Background -> background
+      Underlining -> foreground
     intensity = \case
