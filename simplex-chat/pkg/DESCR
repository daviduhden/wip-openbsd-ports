SimpleX Chat is a terminal-based messaging application that uses
SimpleXMQ as its messaging backend. It provides:

- End-to-end encrypted messaging
- No user identifiers (no phone numbers, no email required)
- Group chats and direct messaging
- File sharing via XFTP
- Multiple user profiles
- Markdown message formatting
- No crypto keys stored on servers (only on client devices)
