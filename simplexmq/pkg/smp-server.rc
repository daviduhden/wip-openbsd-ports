#!/bin/sh
#
# $OpenBSD$
#
# PROVIDE: smp_server
# REQUIRE: DAEMON
# KEYWORD: shutdown

daemon="${TRUEPREFIX}/bin/smp-server"
daemon_user="_simplexmq"
daemon_flags="start"
daemon_logger="daemon.info"

. /etc/rc.d/rc.subr

rc_bg=YES
pexp="smp-server:.*"

rc_cmd $1
