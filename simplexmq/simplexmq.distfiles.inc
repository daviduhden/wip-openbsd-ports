## Hackage sources.
DISTFILES +=		aeson-${AESON_V}-rev${AESON_REV}.cabal{aeson/${AESON_V}/revision/${AESON_REV}.cabal}
DISTFILES +=		aeson-${AESON_V}.tar.gz{aeson/${AESON_V}/tar.gz}
DISTFILES.hs +=		Cabal-${CABAL_V}.tar.gz
DISTFILES.hs +=		Cabal-syntax-${CABAL_SYNTAX_V}.tar.gz
DISTFILES.hs +=		HUnit-${HUNIT_V}.tar.gz
DISTFILES.hs +=		OneTuple-${ONETUPLE_V}.tar.gz
DISTFILES.hs +=		Only-${ONLY_V}-rev${ONLY_REV}.cabal{Only-${ONLY_V}/revision/${ONLY_REV}.cabal}
DISTFILES.hs +=		Only-${ONLY_V}.tar.gz
DISTFILES.hs +=		QuickCheck-${QUICKCHECK_V}.tar.gz
DISTFILES.hs +=		SHA-${SHA_V}.tar.gz
DISTFILES.hs +=		alex-${ALEX_V}.tar.gz
DISTFILES.hs +=		ansi-terminal-${ANSI_TERMINAL_V}.tar.gz
DISTFILES.hs +=		appar-${APPAR_V}.tar.gz
DISTFILES.hs +=		asn1-encoding-${ASN1_ENCODING_V}.tar.gz
DISTFILES.hs +=		asn1-parse-${ASN1_PARSE_V}.tar.gz
DISTFILES.hs +=		asn1-types-${ASN1_TYPES_V}.tar.gz
DISTFILES.hs +=		assoc-${ASSOC_V}.tar.gz
DISTFILES.hs +=		async-${ASYNC_V}.tar.gz
DISTFILES.hs +=		attoparsec-${ATTOPARSEC_V}-rev${ATTOPARSEC_REV}.cabal{attoparsec/${ATTOPARSEC_V}/revision/${ATTOPARSEC_REV}.cabal}
DISTFILES.hs +=		attoparsec-${ATTOPARSEC_V}.tar.gz
DISTFILES.hs +=		attoparsec-iso8601-${ATTOPARSEC_ISO8601_V}.tar.gz
DISTFILES.hs +=		auto-update-${AUTO_UPDATE_V}.tar.gz
DISTFILES.hs +=		base-orphans-${BASE_ORPHANS_V}.tar.gz
DISTFILES.hs +=		base16-bytestring-${BASE16_BYTESTRING_V}.tar.gz
DISTFILES.hs +=		base64-bytestring-${BASE64_BYTESTRING_V}.tar.gz
DISTFILES.hs +=		basement-${BASEMENT_V}.tar.gz
DISTFILES.hs +=		bifunctor-classes-compat-${BIFUNCTOR_CLASSES_COMPAT_V}.tar.gz
DISTFILES.hs +=		binary-${BINARY_V}.tar.gz
DISTFILES.hs +=		bitvec-${BITVEC_V}.tar.gz
DISTFILES.hs +=		blaze-builder-${BLAZE_BUILDER_V}.tar.gz
DISTFILES.hs +=		blaze-html-${BLAZE_HTML_V}.tar.gz
DISTFILES.hs +=		blaze-markup-${BLAZE_MARKUP_V}-rev${BLAZE_MARKUP_REV}.cabal{blaze-markup-${BLAZE_MARKUP_V}/revision/${BLAZE_MARKUP_REV}.cabal}
DISTFILES.hs +=		blaze-markup-${BLAZE_MARKUP_V}.tar.gz
DISTFILES.hs +=		blaze-textual-${BLAZE_TEXTUAL_V}.tar.gz
DISTFILES.hs +=		boring-${BORING_V}.tar.gz
DISTFILES.hs +=		bsb-http-chunked-${BSB_HTTP_CHUNKED_V}.cabal{bsb-http-chunked-${BSB_HTTP_CHUNKED_V}/bsb-http-chunked.cabal}
DISTFILES.hs +=		bsb-http-chunked-${BSB_HTTP_CHUNKED_V}.tar.gz
DISTFILES.hs +=		byteorder-${BYTEORDER_V}.tar.gz
DISTFILES.hs +=		bytestring-${BYTESTRING_V}.tar.gz
DISTFILES.hs +=		call-stack-${CALL_STACK_V}.tar.gz
DISTFILES.hs +=		case-insensitive-${CASE_INSENSITIVE_V}.tar.gz
DISTFILES.hs +=		cborg-${CBORG_V}-rev${CBORG_REV}.cabal{cborg-${CBORG_V}/revision/${CBORG_REV}.cabal}
DISTFILES.hs +=		cborg-${CBORG_V}.tar.gz
DISTFILES.hs +=		cereal-${CEREAL_V}.tar.gz
DISTFILES.hs +=		character-ps-${CHARACTER_PS_V}.tar.gz
DISTFILES.hs +=		clock-${CLOCK_V}.tar.gz
DISTFILES.hs +=		colour-${COLOUR_V}.tar.gz
DISTFILES.hs +=		composition-${COMPOSITION_V}.tar.gz
DISTFILES.hs +=		conduit-${CONDUIT_V}.tar.gz
DISTFILES.hs +=		conduit-extra-${CONDUIT_EXTRA_V}.tar.gz
DISTFILES.hs +=		constraints-${CONSTRAINTS_V}.tar.gz
DISTFILES.hs +=		containers-${CONTAINERS_V}.tar.gz
DISTFILES.hs +=		cookie-${COOKIE_V}.tar.gz
DISTFILES.hs +=		crypto-token-${CRYPTO_TOKEN_V}.tar.gz
DISTFILES.hs +=		crypton-${CRYPTON_V}.tar.gz
DISTFILES.hs +=		crypton-x509-${CRYPTON_X509_V}.tar.gz
DISTFILES.hs +=		crypton-x509-store-${CRYPTON_X509_STORE_V}.tar.gz
DISTFILES.hs +=		crypton-x509-validation-${CRYPTON_X509_VALIDATION_V}.tar.gz
DISTFILES.hs +=		cryptostore-${CRYPTOSTORE_V}.tar.gz
DISTFILES.hs +=		data-array-byte-${DATA_ARRAY_BYTE_V}.tar.gz
DISTFILES.hs +=		data-default-${DATA_DEFAULT_V}.tar.gz
DISTFILES.hs +=		data-default-class-${DATA_DEFAULT_CLASS_V}.tar.gz
DISTFILES.hs +=		data-default-instances-containers-${DATA_DEFAULT_INSTANCES_CONTAINERS_V}.tar.gz
DISTFILES.hs +=		data-default-instances-dlist-${DATA_DEFAULT_INSTANCES_DLIST_V}.tar.gz
DISTFILES.hs +=		data-default-instances-old-locale-${DATA_DEFAULT_INSTANCES_OLD_LOCALE_V}.tar.gz
DISTFILES.hs +=		data-fix-${DATA_FIX_V}.tar.gz
DISTFILES.hs +=		directory-${DIRECTORY_V}.tar.gz
DISTFILES.hs +=		dlist-${DLIST_V}-rev${DLIST_REV}.cabal{dlist-${DLIST_V}/revision/${DLIST_REV}.cabal}
DISTFILES.hs +=		dlist-${DLIST_V}.tar.gz
DISTFILES.hs +=		easy-file-${EASY_FILE_V}.tar.gz
DISTFILES.hs +=		entropy-${ENTROPY_V}.tar.gz
DISTFILES.hs +=		fast-logger-${FAST_LOGGER_V}.tar.gz
DISTFILES.hs +=		file-embed-${FILE_EMBED_V}.tar.gz
DISTFILES.hs +=		file-io-${FILE_IO_V}.tar.gz
DISTFILES.hs +=		filepath-${FILEPATH_V}.tar.gz
DISTFILES.hs +=		foldable1-classes-compat-${FOLDABLE1_CLASSES_COMPAT_V}.tar.gz
DISTFILES.hs +=		generic-random-${GENERIC_RANDOM_V}.tar.gz
DISTFILES.hs +=		generically-${GENERICALLY_V}-rev${GENERICALLY_REV}.cabal{generically-${GENERICALLY_V}/revision/${GENERICALLY_REV}.cabal}
DISTFILES.hs +=		generically-${GENERICALLY_V}.tar.gz
DISTFILES.hs +=		ghc-bignum-orphans-${GHC_BIGNUM_ORPHANS_V}-rev${GHC_BIGNUM_ORPHANS_REV}.cabal{ghc-bignum-orphans-${GHC_BIGNUM_ORPHANS_V}/revision/${GHC_BIGNUM_ORPHANS_REV}.cabal}
DISTFILES.hs +=		ghc-bignum-orphans-${GHC_BIGNUM_ORPHANS_V}.tar.gz
DISTFILES.hs +=		half-${HALF_V}.tar.gz
DISTFILES.hs +=		hashable-${HASHABLE_V}.tar.gz
DISTFILES.hs +=		hashmap-${HASHMAP_V}.tar.gz
DISTFILES.hs +=		haskell-lexer-${HASKELL_LEXER_V}.tar.gz
DISTFILES.hs +=		hourglass-${HOURGLASS_V}.tar.gz
DISTFILES.hs +=		hsc2hs-${HSC2HS_V}-rev${HSC2HS_REV}.cabal{hsc2hs-${HSC2HS_V}/revision/${HSC2HS_REV}.cabal}
DISTFILES.hs +=		hsc2hs-${HSC2HS_V}.tar.gz
DISTFILES.hs +=		hspec-${HSPEC_V}.tar.gz
DISTFILES.hs +=		hspec-core-${HSPEC_CORE_V}.tar.gz
DISTFILES.hs +=		hspec-discover-${HSPEC_DISCOVER_V}.tar.gz
DISTFILES.hs +=		hspec-expectations-${HSPEC_EXPECTATIONS_V}.tar.gz
DISTFILES.hs +=		http-client-${HTTP_CLIENT_V}.tar.gz
DISTFILES.hs +=		http-date-${HTTP_DATE_V}.tar.gz
DISTFILES.hs +=		http-semantics-${HTTP_SEMANTICS_V}.tar.gz
DISTFILES.hs +=		http-types-${HTTP_TYPES_V}.tar.gz
DISTFILES.hs +=		http2-${HTTP2_V}.tar.gz
DISTFILES.hs +=		indexed-traversable-${INDEXED_TRAVERSABLE_V}.tar.gz
DISTFILES.hs +=		indexed-traversable-instances-${INDEXED_TRAVERSABLE_INSTANCES_V}.tar.gz
DISTFILES.hs +=		ini-${INI_V}.tar.gz
DISTFILES.hs +=		integer-conversion-${INTEGER_CONVERSION_V}.tar.gz
DISTFILES.hs +=		integer-logarithms-${INTEGER_LOGARITHMS_V}.tar.gz
DISTFILES.hs +=		iproute-${IPROUTE_V}.tar.gz
DISTFILES.hs +=		iso8601-time-${ISO8601_TIME_V}.tar.gz
DISTFILES.hs +=		libyaml-${LIBYAML_V}.tar.gz
DISTFILES.hs +=		libyaml-clib-${LIBYAML_CLIB_V}.tar.gz
DISTFILES.hs +=		lifted-base-${LIFTED_BASE_V}.tar.gz
DISTFILES.hs +=		main-tester-${MAIN_TESTER_V}.tar.gz
DISTFILES.hs +=		memory-${MEMORY_V}.tar.gz
DISTFILES.hs +=		mime-types-${MIME_TYPES_V}.tar.gz
DISTFILES.hs +=		monad-control-${MONAD_CONTROL_V}.tar.gz
DISTFILES.hs +=		monad-logger-${MONAD_LOGGER_V}.tar.gz
DISTFILES.hs +=		monad-loops-${MONAD_LOOPS_V}.tar.gz
DISTFILES.hs +=		mono-traversable-${MONO_TRAVERSABLE_V}.tar.gz
DISTFILES.hs +=		network-${NETWORK_V}.tar.gz
DISTFILES.hs +=		network-byte-order-${NETWORK_BYTE_ORDER_V}.tar.gz
DISTFILES.hs +=		network-control-${NETWORK_CONTROL_V}.tar.gz
DISTFILES.hs +=		network-info-${NETWORK_INFO_V}.tar.gz
DISTFILES.hs +=		network-transport-${NETWORK_TRANSPORT_V}.tar.gz
DISTFILES.hs +=		network-udp-${NETWORK_UDP_V}.tar.gz
DISTFILES.hs +=		network-uri-${NETWORK_URI_V}-rev${NETWORK_URI_REV}.cabal{network-uri-${NETWORK_URI_V}/revision/${NETWORK_URI_REV}.cabal}
DISTFILES.hs +=		network-uri-${NETWORK_URI_V}.tar.gz
DISTFILES.hs +=		old-locale-${OLD_LOCALE_V}.tar.gz
DISTFILES.hs +=		old-time-${OLD_TIME_V}.tar.gz
DISTFILES.hs +=		optparse-applicative-${OPTPARSE_APPLICATIVE_V}.tar.gz
DISTFILES.hs +=		os-string-${OS_STRING_V}.tar.gz
DISTFILES.hs +=		parsec-${PARSEC_V}.tar.gz
DISTFILES.hs +=		pem-${PEM_V}.tar.gz
DISTFILES.hs +=		prettyprinter-${PRETTYPRINTER_V}.tar.gz
DISTFILES.hs +=		prettyprinter-ansi-terminal-${PRETTYPRINTER_ANSI_TERMINAL_V}.tar.gz
DISTFILES.hs +=		primitive-${PRIMITIVE_V}.tar.gz
DISTFILES.hs +=		process-${PROCESS_V}.tar.gz
DISTFILES.hs +=		psqueues-${PSQUEUES_V}.tar.gz
DISTFILES.hs +=		quickcheck-io-${QUICKCHECK_IO_V}.tar.gz
DISTFILES.hs +=		random-${RANDOM_V}.tar.gz
DISTFILES.hs +=		recv-${RECV_V}.tar.gz
DISTFILES.hs +=		resourcet-${RESOURCET_V}.tar.gz
DISTFILES.hs +=		safe-exceptions-${SAFE_EXCEPTIONS_V}-rev${SAFE_EXCEPTIONS_REV}.cabal{safe-exceptions-${SAFE_EXCEPTIONS_V}/revision/${SAFE_EXCEPTIONS_REV}.cabal}
DISTFILES.hs +=		safe-exceptions-${SAFE_EXCEPTIONS_V}.tar.gz
DISTFILES.hs +=		scientific-${SCIENTIFIC_V}.tar.gz
DISTFILES.hs +=		semialign-${SEMIALIGN_V}.tar.gz
DISTFILES.hs +=		serialise-${SERIALISE_V}-rev${SERIALISE_REV}.cabal{serialise-${SERIALISE_V}/revision/${SERIALISE_REV}.cabal}
DISTFILES.hs +=		serialise-${SERIALISE_V}.tar.gz
DISTFILES.hs +=		silently-${SILENTLY_V}.tar.gz
DISTFILES.hs +=		simple-logger-${SIMPLE_LOGGER_V}.tar.gz
DISTFILES.hs +=		simple-sendfile-${SIMPLE_SENDFILE_V}.tar.gz
DISTFILES.hs +=		socks-${SOCKS_V}.tar.gz
DISTFILES.hs +=		split-${SPLIT_V}.tar.gz
DISTFILES.hs +=		splitmix-${SPLITMIX_V}.tar.gz
DISTFILES.hs +=		stm-chans-${STM_CHANS_V}.tar.gz
DISTFILES.hs +=		streaming-commons-${STREAMING_COMMONS_V}.tar.gz
DISTFILES.hs +=		strict-${STRICT_V}.tar.gz
DISTFILES.hs +=		tagged-${TAGGED_V}.tar.gz
DISTFILES.hs +=		tasty-${TASTY_V}.tar.gz
DISTFILES.hs +=		tasty-bench-${TASTY_BENCH_V}.tar.gz
DISTFILES.hs +=		temporary-${TEMPORARY_V}.tar.gz
DISTFILES.hs +=		text-${TEXT_V}.tar.gz
DISTFILES.hs +=		text-iso8601-${TEXT_ISO8601_V}.tar.gz
DISTFILES.hs +=		text-short-${TEXT_SHORT_V}.tar.gz
DISTFILES.hs +=		th-abstraction-${TH_ABSTRACTION_V}.tar.gz
DISTFILES.hs +=		th-compat-${TH_COMPAT_V}.tar.gz
DISTFILES.hs +=		these-${THESE_V}.tar.gz
DISTFILES.hs +=		time-compat-${TIME_COMPAT_V}.tar.gz
DISTFILES.hs +=		time-manager-${TIME_MANAGER_V}.tar.gz
DISTFILES.hs +=		timeit-${TIMEIT_V}.tar.gz
DISTFILES.hs +=		tls-${TLS_V}.tar.gz
DISTFILES.hs +=		tls-session-manager-${TLS_SESSION_MANAGER_V}.tar.gz
DISTFILES.hs +=		transformers-base-${TRANSFORMERS_BASE_V}.tar.gz
DISTFILES.hs +=		transformers-compat-${TRANSFORMERS_COMPAT_V}.tar.gz
DISTFILES.hs +=		typed-process-${TYPED_PROCESS_V}.tar.gz
DISTFILES.hs +=		unix-${UNIX_V}.tar.gz
DISTFILES.hs +=		unix-compat-${UNIX_COMPAT_V}.tar.gz
DISTFILES.hs +=		unix-time-${UNIX_TIME_V}.tar.gz
DISTFILES.hs +=		unliftio-${UNLIFTIO_V}.tar.gz
DISTFILES.hs +=		unliftio-core-${UNLIFTIO_CORE_V}-rev${UNLIFTIO_CORE_REV}.cabal{unliftio-core-${UNLIFTIO_CORE_V}/revision/${UNLIFTIO_CORE_REV}.cabal}
DISTFILES.hs +=		unliftio-core-${UNLIFTIO_CORE_V}.tar.gz
DISTFILES.hs +=		unordered-containers-${UNORDERED_CONTAINERS_V}.tar.gz
DISTFILES.hs +=		utf8-string-${UTF8_STRING_V}.tar.gz
DISTFILES.hs +=		uuid-types-${UUID_TYPES_V}.tar.gz
DISTFILES.hs +=		vault-${VAULT_V}.tar.gz
DISTFILES.hs +=		vector-${VECTOR_V}.tar.gz
DISTFILES.hs +=		vector-algorithms-${VECTOR_ALGORITHMS_V}.tar.gz
DISTFILES.hs +=		vector-stream-${VECTOR_STREAM_V}-rev${VECTOR_STREAM_REV}.cabal{vector-stream-${VECTOR_STREAM_V}/revision/${VECTOR_STREAM_REV}.cabal}
DISTFILES.hs +=		vector-stream-${VECTOR_STREAM_V}.tar.gz
DISTFILES.hs +=		wai-${WAI_V}.tar.gz
DISTFILES.hs +=		wai-app-static-${WAI_APP_STATIC_V}.tar.gz
DISTFILES.hs +=		wai-extra-${WAI_EXTRA_V}.tar.gz
DISTFILES.hs +=		wai-logger-${WAI_LOGGER_V}.tar.gz
DISTFILES.hs +=		warp-${WARP_V}.tar.gz
DISTFILES.hs +=		warp-tls-${WARP_TLS_V}.tar.gz
DISTFILES.hs +=		websockets-${WEBSOCKETS_V}.tar.gz
DISTFILES.hs +=		witherable-${WITHERABLE_V}.tar.gz
DISTFILES.hs +=		word8-${WORD8_V}.tar.gz
DISTFILES.hs +=		yaml-${YAML_V}.tar.gz
DISTFILES.hs +=		zlib-${ZLIB_V}.tar.gz
DISTFILES.hs +=		zstd-${ZSTD_V}.tar.gz
## GitHub sources.
DISTFILES +=		direct-sqlcipher-f814ee68b16a9447fbb467ccc8f29bdd3546bfd9${EXTRACT_SUFX}{direct-sqlcipher/tar.gz/f814ee68b16a9447fbb467ccc8f29bdd3546bfd9}
DISTFILES +=		hs-socks-a30cc7a79a08d8108316094f8f2f82a0c5e1ac51${EXTRACT_SUFX}{hs-socks/tar.gz/a30cc7a79a08d8108316094f8f2f82a0c5e1ac51}
DISTFILES +=		sqlcipher-simple-a46bd361a19376c5211f1058908fc0ae6bf42446${EXTRACT_SUFX}{sqlcipher-simple/tar.gz/a46bd361a19376c5211f1058908fc0ae6bf42446}
## Hackage sources.
EXTRACT_ONLY +=		Cabal-${CABAL_V}.tar.gz
EXTRACT_ONLY +=		Cabal-syntax-${CABAL_SYNTAX_V}.tar.gz
EXTRACT_ONLY +=		HUnit-${HUNIT_V}.tar.gz
EXTRACT_ONLY +=		OneTuple-${ONETUPLE_V}.tar.gz
EXTRACT_ONLY +=		Only-${ONLY_V}.tar.gz
EXTRACT_ONLY +=		QuickCheck-${QUICKCHECK_V}.tar.gz
EXTRACT_ONLY +=		SHA-${SHA_V}.tar.gz
EXTRACT_ONLY +=		aeson-${AESON_V}.tar.gz
EXTRACT_ONLY +=		alex-${ALEX_V}.tar.gz
EXTRACT_ONLY +=		ansi-terminal-${ANSI_TERMINAL_V}.tar.gz
EXTRACT_ONLY +=		appar-${APPAR_V}.tar.gz
EXTRACT_ONLY +=		asn1-encoding-${ASN1_ENCODING_V}.tar.gz
EXTRACT_ONLY +=		asn1-parse-${ASN1_PARSE_V}.tar.gz
EXTRACT_ONLY +=		asn1-types-${ASN1_TYPES_V}.tar.gz
EXTRACT_ONLY +=		assoc-${ASSOC_V}.tar.gz
EXTRACT_ONLY +=		async-${ASYNC_V}.tar.gz
EXTRACT_ONLY +=		attoparsec-${ATTOPARSEC_V}.tar.gz
EXTRACT_ONLY +=		attoparsec-iso8601-${ATTOPARSEC_ISO8601_V}.tar.gz
EXTRACT_ONLY +=		auto-update-${AUTO_UPDATE_V}.tar.gz
EXTRACT_ONLY +=		base-orphans-${BASE_ORPHANS_V}.tar.gz
EXTRACT_ONLY +=		base16-bytestring-${BASE16_BYTESTRING_V}.tar.gz
EXTRACT_ONLY +=		base64-bytestring-${BASE64_BYTESTRING_V}.tar.gz
EXTRACT_ONLY +=		basement-${BASEMENT_V}.tar.gz
EXTRACT_ONLY +=		bifunctor-classes-compat-${BIFUNCTOR_CLASSES_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		binary-${BINARY_V}.tar.gz
EXTRACT_ONLY +=		bitvec-${BITVEC_V}.tar.gz
EXTRACT_ONLY +=		blaze-builder-${BLAZE_BUILDER_V}.tar.gz
EXTRACT_ONLY +=		blaze-html-${BLAZE_HTML_V}.tar.gz
EXTRACT_ONLY +=		blaze-markup-${BLAZE_MARKUP_V}.tar.gz
EXTRACT_ONLY +=		blaze-textual-${BLAZE_TEXTUAL_V}.tar.gz
EXTRACT_ONLY +=		boring-${BORING_V}.tar.gz
EXTRACT_ONLY +=		bsb-http-chunked-${BSB_HTTP_CHUNKED_V}.tar.gz
EXTRACT_ONLY +=		byteorder-${BYTEORDER_V}.tar.gz
EXTRACT_ONLY +=		bytestring-${BYTESTRING_V}.tar.gz
EXTRACT_ONLY +=		call-stack-${CALL_STACK_V}.tar.gz
EXTRACT_ONLY +=		case-insensitive-${CASE_INSENSITIVE_V}.tar.gz
EXTRACT_ONLY +=		cborg-${CBORG_V}.tar.gz
EXTRACT_ONLY +=		cereal-${CEREAL_V}.tar.gz
EXTRACT_ONLY +=		character-ps-${CHARACTER_PS_V}.tar.gz
EXTRACT_ONLY +=		clock-${CLOCK_V}.tar.gz
EXTRACT_ONLY +=		colour-${COLOUR_V}.tar.gz
EXTRACT_ONLY +=		composition-${COMPOSITION_V}.tar.gz
EXTRACT_ONLY +=		conduit-${CONDUIT_V}.tar.gz
EXTRACT_ONLY +=		conduit-extra-${CONDUIT_EXTRA_V}.tar.gz
EXTRACT_ONLY +=		constraints-${CONSTRAINTS_V}.tar.gz
EXTRACT_ONLY +=		containers-${CONTAINERS_V}.tar.gz
EXTRACT_ONLY +=		cookie-${COOKIE_V}.tar.gz
EXTRACT_ONLY +=		crypto-token-${CRYPTO_TOKEN_V}.tar.gz
EXTRACT_ONLY +=		crypton-${CRYPTON_V}.tar.gz
EXTRACT_ONLY +=		crypton-x509-${CRYPTON_X509_V}.tar.gz
EXTRACT_ONLY +=		crypton-x509-store-${CRYPTON_X509_STORE_V}.tar.gz
EXTRACT_ONLY +=		crypton-x509-validation-${CRYPTON_X509_VALIDATION_V}.tar.gz
EXTRACT_ONLY +=		cryptostore-${CRYPTOSTORE_V}.tar.gz
EXTRACT_ONLY +=		data-array-byte-${DATA_ARRAY_BYTE_V}.tar.gz
EXTRACT_ONLY +=		data-default-${DATA_DEFAULT_V}.tar.gz
EXTRACT_ONLY +=		data-default-class-${DATA_DEFAULT_CLASS_V}.tar.gz
EXTRACT_ONLY +=		data-default-instances-containers-${DATA_DEFAULT_INSTANCES_CONTAINERS_V}.tar.gz
EXTRACT_ONLY +=		data-default-instances-dlist-${DATA_DEFAULT_INSTANCES_DLIST_V}.tar.gz
EXTRACT_ONLY +=		data-fix-${DATA_FIX_V}.tar.gz
EXTRACT_ONLY +=		directory-${DIRECTORY_V}.tar.gz
EXTRACT_ONLY +=		dlist-${DLIST_V}.tar.gz
EXTRACT_ONLY +=		easy-file-${EASY_FILE_V}.tar.gz
EXTRACT_ONLY +=		entropy-${ENTROPY_V}.tar.gz
EXTRACT_ONLY +=		fast-logger-${FAST_LOGGER_V}.tar.gz
EXTRACT_ONLY +=		file-embed-${FILE_EMBED_V}.tar.gz
EXTRACT_ONLY +=		file-io-${FILE_IO_V}.tar.gz
EXTRACT_ONLY +=		filepath-${FILEPATH_V}.tar.gz
EXTRACT_ONLY +=		foldable1-classes-compat-${FOLDABLE1_CLASSES_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		generic-random-${GENERIC_RANDOM_V}.tar.gz
EXTRACT_ONLY +=		generically-${GENERICALLY_V}.tar.gz
EXTRACT_ONLY +=		ghc-bignum-orphans-${GHC_BIGNUM_ORPHANS_V}.tar.gz
EXTRACT_ONLY +=		half-${HALF_V}.tar.gz
EXTRACT_ONLY +=		hashable-${HASHABLE_V}.tar.gz
EXTRACT_ONLY +=		hashmap-${HASHMAP_V}.tar.gz
EXTRACT_ONLY +=		haskell-lexer-${HASKELL_LEXER_V}.tar.gz
EXTRACT_ONLY +=		hourglass-${HOURGLASS_V}.tar.gz
EXTRACT_ONLY +=		hsc2hs-${HSC2HS_V}.tar.gz
EXTRACT_ONLY +=		hspec-${HSPEC_V}.tar.gz
EXTRACT_ONLY +=		hspec-core-${HSPEC_CORE_V}.tar.gz
EXTRACT_ONLY +=		hspec-discover-${HSPEC_DISCOVER_V}.tar.gz
EXTRACT_ONLY +=		hspec-expectations-${HSPEC_EXPECTATIONS_V}.tar.gz
EXTRACT_ONLY +=		http-client-${HTTP_CLIENT_V}.tar.gz
EXTRACT_ONLY +=		http-date-${HTTP_DATE_V}.tar.gz
EXTRACT_ONLY +=		http-semantics-${HTTP_SEMANTICS_V}.tar.gz
EXTRACT_ONLY +=		http-types-${HTTP_TYPES_V}.tar.gz
EXTRACT_ONLY +=		http2-${HTTP2_V}.tar.gz
EXTRACT_ONLY +=		indexed-traversable-${INDEXED_TRAVERSABLE_V}.tar.gz
EXTRACT_ONLY +=		indexed-traversable-instances-${INDEXED_TRAVERSABLE_INSTANCES_V}.tar.gz
EXTRACT_ONLY +=		ini-${INI_V}.tar.gz
EXTRACT_ONLY +=		integer-conversion-${INTEGER_CONVERSION_V}.tar.gz
EXTRACT_ONLY +=		integer-logarithms-${INTEGER_LOGARITHMS_V}.tar.gz
EXTRACT_ONLY +=		iproute-${IPROUTE_V}.tar.gz
EXTRACT_ONLY +=		iso8601-time-${ISO8601_TIME_V}.tar.gz
EXTRACT_ONLY +=		libyaml-${LIBYAML_V}.tar.gz
EXTRACT_ONLY +=		libyaml-clib-${LIBYAML_CLIB_V}.tar.gz
EXTRACT_ONLY +=		lifted-base-${LIFTED_BASE_V}.tar.gz
EXTRACT_ONLY +=		main-tester-${MAIN_TESTER_V}.tar.gz
EXTRACT_ONLY +=		memory-${MEMORY_V}.tar.gz
EXTRACT_ONLY +=		mime-types-${MIME_TYPES_V}.tar.gz
EXTRACT_ONLY +=		monad-control-${MONAD_CONTROL_V}.tar.gz
EXTRACT_ONLY +=		monad-logger-${MONAD_LOGGER_V}.tar.gz
EXTRACT_ONLY +=		monad-loops-${MONAD_LOOPS_V}.tar.gz
EXTRACT_ONLY +=		mono-traversable-${MONO_TRAVERSABLE_V}.tar.gz
EXTRACT_ONLY +=		network-${NETWORK_V}.tar.gz
EXTRACT_ONLY +=		network-byte-order-${NETWORK_BYTE_ORDER_V}.tar.gz
EXTRACT_ONLY +=		network-control-${NETWORK_CONTROL_V}.tar.gz
EXTRACT_ONLY +=		network-info-${NETWORK_INFO_V}.tar.gz
EXTRACT_ONLY +=		network-transport-${NETWORK_TRANSPORT_V}.tar.gz
EXTRACT_ONLY +=		network-udp-${NETWORK_UDP_V}.tar.gz
EXTRACT_ONLY +=		network-uri-${NETWORK_URI_V}.tar.gz
EXTRACT_ONLY +=		optparse-applicative-${OPTPARSE_APPLICATIVE_V}.tar.gz
EXTRACT_ONLY +=		os-string-${OS_STRING_V}.tar.gz
EXTRACT_ONLY +=		parsec-${PARSEC_V}.tar.gz
EXTRACT_ONLY +=		pem-${PEM_V}.tar.gz
EXTRACT_ONLY +=		prettyprinter-${PRETTYPRINTER_V}.tar.gz
EXTRACT_ONLY +=		prettyprinter-ansi-terminal-${PRETTYPRINTER_ANSI_TERMINAL_V}.tar.gz
EXTRACT_ONLY +=		primitive-${PRIMITIVE_V}.tar.gz
EXTRACT_ONLY +=		process-${PROCESS_V}.tar.gz
EXTRACT_ONLY +=		psqueues-${PSQUEUES_V}.tar.gz
EXTRACT_ONLY +=		quickcheck-io-${QUICKCHECK_IO_V}.tar.gz
EXTRACT_ONLY +=		random-${RANDOM_V}.tar.gz
EXTRACT_ONLY +=		recv-${RECV_V}.tar.gz
EXTRACT_ONLY +=		resourcet-${RESOURCET_V}.tar.gz
EXTRACT_ONLY +=		safe-exceptions-${SAFE_EXCEPTIONS_V}.tar.gz
EXTRACT_ONLY +=		scientific-${SCIENTIFIC_V}.tar.gz
EXTRACT_ONLY +=		semialign-${SEMIALIGN_V}.tar.gz
EXTRACT_ONLY +=		serialise-${SERIALISE_V}.tar.gz
EXTRACT_ONLY +=		silently-${SILENTLY_V}.tar.gz
EXTRACT_ONLY +=		simple-logger-${SIMPLE_LOGGER_V}.tar.gz
EXTRACT_ONLY +=		simple-sendfile-${SIMPLE_SENDFILE_V}.tar.gz
EXTRACT_ONLY +=		split-${SPLIT_V}.tar.gz
EXTRACT_ONLY +=		splitmix-${SPLITMIX_V}.tar.gz
EXTRACT_ONLY +=		stm-chans-${STM_CHANS_V}.tar.gz
EXTRACT_ONLY +=		streaming-commons-${STREAMING_COMMONS_V}.tar.gz
EXTRACT_ONLY +=		strict-${STRICT_V}.tar.gz
EXTRACT_ONLY +=		tagged-${TAGGED_V}.tar.gz
EXTRACT_ONLY +=		tasty-${TASTY_V}.tar.gz
EXTRACT_ONLY +=		tasty-bench-${TASTY_BENCH_V}.tar.gz
EXTRACT_ONLY +=		temporary-${TEMPORARY_V}.tar.gz
EXTRACT_ONLY +=		text-${TEXT_V}.tar.gz
EXTRACT_ONLY +=		text-iso8601-${TEXT_ISO8601_V}.tar.gz
EXTRACT_ONLY +=		text-short-${TEXT_SHORT_V}.tar.gz
EXTRACT_ONLY +=		th-abstraction-${TH_ABSTRACTION_V}.tar.gz
EXTRACT_ONLY +=		th-compat-${TH_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		these-${THESE_V}.tar.gz
EXTRACT_ONLY +=		time-compat-${TIME_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		time-manager-${TIME_MANAGER_V}.tar.gz
EXTRACT_ONLY +=		timeit-${TIMEIT_V}.tar.gz
EXTRACT_ONLY +=		tls-session-manager-${TLS_SESSION_MANAGER_V}.tar.gz
EXTRACT_ONLY +=		transformers-base-${TRANSFORMERS_BASE_V}.tar.gz
EXTRACT_ONLY +=		transformers-compat-${TRANSFORMERS_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		typed-process-${TYPED_PROCESS_V}.tar.gz
EXTRACT_ONLY +=		unix-${UNIX_V}.tar.gz
EXTRACT_ONLY +=		unix-compat-${UNIX_COMPAT_V}.tar.gz
EXTRACT_ONLY +=		unix-time-${UNIX_TIME_V}.tar.gz
EXTRACT_ONLY +=		unliftio-${UNLIFTIO_V}.tar.gz
EXTRACT_ONLY +=		unliftio-core-${UNLIFTIO_CORE_V}.tar.gz
EXTRACT_ONLY +=		unordered-containers-${UNORDERED_CONTAINERS_V}.tar.gz
EXTRACT_ONLY +=		utf8-string-${UTF8_STRING_V}.tar.gz
EXTRACT_ONLY +=		uuid-types-${UUID_TYPES_V}.tar.gz
EXTRACT_ONLY +=		vault-${VAULT_V}.tar.gz
EXTRACT_ONLY +=		vector-${VECTOR_V}.tar.gz
EXTRACT_ONLY +=		vector-algorithms-${VECTOR_ALGORITHMS_V}.tar.gz
EXTRACT_ONLY +=		vector-stream-${VECTOR_STREAM_V}.tar.gz
EXTRACT_ONLY +=		wai-${WAI_V}.tar.gz
EXTRACT_ONLY +=		wai-app-static-${WAI_APP_STATIC_V}.tar.gz
EXTRACT_ONLY +=		wai-extra-${WAI_EXTRA_V}.tar.gz
EXTRACT_ONLY +=		wai-logger-${WAI_LOGGER_V}.tar.gz
EXTRACT_ONLY +=		warp-${WARP_V}.tar.gz
EXTRACT_ONLY +=		warp-tls-${WARP_TLS_V}.tar.gz
EXTRACT_ONLY +=		websockets-${WEBSOCKETS_V}.tar.gz
EXTRACT_ONLY +=		witherable-${WITHERABLE_V}.tar.gz
EXTRACT_ONLY +=		word8-${WORD8_V}.tar.gz
EXTRACT_ONLY +=		yaml-${YAML_V}.tar.gz
EXTRACT_ONLY +=		zlib-${ZLIB_V}.tar.gz
EXTRACT_ONLY +=		zstd-${ZSTD_V}.tar.gz
## GitHub sources.
EXTRACT_ONLY +=		direct-sqlcipher-f814ee68b16a9447fbb467ccc8f29bdd3546bfd9${EXTRACT_SUFX}
EXTRACT_ONLY +=		hs-socks-a30cc7a79a08d8108316094f8f2f82a0c5e1ac51${EXTRACT_SUFX}
EXTRACT_ONLY +=		sqlcipher-simple-a46bd361a19376c5211f1058908fc0ae6bf42446${EXTRACT_SUFX}
