uutils sed is a cross-platform reimplementation of sed in Rust.

This package installs ${PREFIX}/bin/gsed, the name used by
textproc/gsed.  It conflicts with textproc/gsed and declares it as a
secondary pkgpath, so the package tools accept it as a provider of that
path.  The GNU package must not be installed at the same time.
